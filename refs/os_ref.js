// Operation System
const os = require(`os`);

// Определяет ОС
console.log(os.platform());

// Архитектура
console.log(os.arch());

// Информация
console.log(os.cpus());

// Свободная память
console.log(os.freemem());

// Свободная память
console.log(os.totalmem());

// Корневая директория на ПК
console.log(os.homedir());

// Скотлько система работает

console.log(os.uptime());
